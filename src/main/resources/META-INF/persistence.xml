<?xml version="1.0" encoding="UTF-8"?>
<!-- Persistence unit configuration.
     This file configures a JPA provider, in this case Hibernate. The file is
     expected to be named persistence.xml and located in 
     src/main/resources/META-INF/ -->

<persistence xmlns="http://java.sun.com/xml/ns/persistence"
 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
 xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd"
 version="1.0">

<!--  The persistence unit can be referred to in code by its name, "auditorPU"
      in this case. -->
 <persistence-unit name="auditorPU" transaction-type="RESOURCE_LOCAL">
  <provider>org.hibernate.ejb.HibernatePersistence</provider>

  <properties>
   <!-- Database connection properties. -->
   <property name="hibernate.connection.driver_class" value="org.h2.Driver" />
   <property name="hibernate.connection.url" value="jdbc:h2:~/test;mv_store=false" />
   <property name="hibernate.connection.username" value ="sa" />
   <property name="hibernate.connection.password" value = "sa" />
   
   <!-- When set to true, hibernate.show_sql prints, to stdout, the SQL that it
        executes. This is useful during development and debugging. -->
   <property name="hibernate.show_sql" value="false" />
   <property name="hibernate.format_sql" value="true" />
   
   <!-- Since most database servers differ in their SQL syntax, Hibernate needs
        to know which SQL dialect to use. -->
   <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect" />
   
   <!-- When set to update, hibernate.hbm2ddl.auto causes Hibernate to update
        generated DDL when it starts. This is useful during development. -->
   <property name="hibernate.hbm2ddl.auto" value="update" />
   
   <!-- Stores the DDL that is generated by Hibernate. The DDL is stored in:
        target/jpa/sql. -->
   <property name="javax.persistence.schema-generation.scripts.action" value="create"/>
   <property name="javax.persistence.schema-generation.create-source" value="metadata"/>
   <property name="javax.persistence.schema-generation.scripts.create-target" value="target/jpa/sql/create-schema.sql"/>
  </properties>
 </persistence-unit>
</persistence>